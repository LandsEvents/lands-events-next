/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["lands-events-laravel.test", "landsevents.test"],
    },
    server: "https://lands-events-laravel.test"
};
export default nextConfig;
